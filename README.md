# Frontend Assignment - Tailwebs

## Task 1: Responsive Landing Page (HTML + CSS + Bootstrap)

**Location:** `task1/index.html`

Simply open `task1/index.html` in a browser. No build step needed.

**Features:**
- Responsive layout (desktop + mobile) using Bootstrap 5
- Hero section, Feature cards, CTA section, Footer
- Semantic HTML, DM Sans font
- Hover animations and transitions
- Dummy images from placehold.co

---

## Task 2: React Landing Page with Product Listing

**Location:** `task2/`

### How to run:
```bash
cd task2
npm install
npm start
```

**Features:**
- React.js with functional components + hooks
- Product cards (image, name, price)
- Search bar (filter by name)
- Category filter (dropdown)
- Loading state (spinner on initial load)
- "No results found" state
- Clean UI with Bootstrap 5
